# --- secrets (NEVER commit) ---
.graph_token
.graph_user_id
.graph_app_secret
.graph_app_id
.gh_pat
cookies.txt

# --- heavy / regenerable artifacts ---
.venv/
models/
videos/
__pycache__/
*.pyc

# --- runner scratch ---
.DS_Store

# NOTE: state files ARE committed on purpose so dedup survives across runs:
#   .fetched_urls, .transcribed, .discovered, inbox.txt,
#   swipe_file.md, reels_index.json

# Local D1 backups (may contain PII) — never commit
backups/
